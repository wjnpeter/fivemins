****Deploy

1. Install firebase CLI 
npm install -g firebase-tools

2. Change [project-id] in file .firebaserc

3. cd to the root directory, run:
cd functions
npm install
npm run build

cd ..
npm install
npm run build

firebase login
firebase deploy

****Debug Locally



****If I change DB's key?
